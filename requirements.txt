# =============================================================================
# requirements.txt - 필요 라이브러리
# =============================================================================
# 설치 방법: pip install -r requirements.txt
# =============================================================================

# 웹 대시보드 프레임워크
streamlit>=1.28.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# 시각화
plotly>=5.18.0

# 파일 감시 (선택사항 - 자동 새로고침용)
watchdog>=3.0.0

# 엑셀 파일 처리 (xlsm 매크로 파일 지원)
xlrd>=2.0.0
